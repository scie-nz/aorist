---
kind: pipeline
type: kubernetes
name: default

steps:
- name: compile-release
  image: docker.io/scienz/aorist-build:20210518b
  commands:
  - cargo build --release

trigger:
  event:
    include:
    - push
